# Backend Dockerfile
# Builds and runs the Node/Express API

FROM node:18-alpine

WORKDIR /app

# Copy only server package files first for better layer caching
COPY package*.json ./

# Install production dependencies
RUN npm ci --omit=dev

# Copy the rest of the source
COPY . .

# Expose API port
EXPOSE 8000

# Start the server
CMD ["npm", "start"]
